/**
 * Scenario 3
 * 
 * Input: Array of integers
 *   Ex: [19, 4, 41, 53, 2, 1]
 * Output: Input sorted from smallest to largest; inclusive
 * @param { Array<number> } arrayToBeSorted Array of integeres
 * @returns The sorted input as an array
 */
export function sortOneDimensionalNumberArray(inputArray: Array<number>): Array<number> {
    // Code goes here
}

/**
 * Scenario 4
 * 
 * Input: Array containing nested arrays of integers
 *   Ex: [[20, 5], [6, 1, 10, 9, 5], [800]];
 * Output: Nested arrays values  from smallest to largest integer; sort the nested arrays by length
 *   Ex: [[800], [5, 20], [1, 5, 6, 9, 10]];
 * @param { Array<Array<number>> } arrayToBeSorted Array of integerS
 * @returns The sorted arrays respectively
 */
export function sortNestedIntegerArrays(inputArray: Array<Array<number>>): Array<Array<number>> {
    // Code goes here
}